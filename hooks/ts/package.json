{
  "name": "claude-hooks",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/*.ts --outdir ./dist --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
